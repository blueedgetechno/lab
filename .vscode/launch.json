{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch local server",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/vintage-tv/serve.cjs",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "skipFiles": ["<node_internals>/**"]
    }
  ]
}